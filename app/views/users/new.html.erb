<h1>Sign-Up</h1>
<div>
  <%= form_for(@user) do |f| %>
     <%= f.label :name %>
     <%= f.text_field :name %>
  
     <%= f.label :email %>
     <%= f.text_field :email %>

     <%= f.submit "Create account", class: "button" %>

  <% end %>
 

</div>
